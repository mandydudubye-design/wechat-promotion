{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && cd ../frontend && npm install && npm run build && xcopy /E /I /Y dist ..\\backend\\public && cd ../backend && npm run build",
    "watchPatterns": [
      "backend/src/**",
      "frontend/src/**"
    ]
  },
  "deploy": {
    "healthcheckPath": "/health",
    "startCommand": "cd backend && npm start"
  }
}