{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm install && cd backend && npm install && npm run build && cd .. && npm start",
    "healthcheckPath": "/health"
  }
}
